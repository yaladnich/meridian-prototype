# Meridian Parts — прототип редизайну каталогу

Бриф для нової сесії: що це, як влаштовано, що зроблено, як перевіряти. Після кожної правки оновлювати
відповідний розділ і рядок у «Журналі версій» унизу.

## Що це

Прототип дизайну каталогу запчастин для сільгосптехніки [meridianparts.com.ua](https://www.meridianparts.com.ua/)
(склад-магазин у Києві). На головній поточного сайту частина блоків — тексти-заглушки шаблону, тому тексти
прототипу беруться з реальних даних магазину (каталог, умови доставки й оплати, послуги).

| | |
|---|---|
| v2 — головна | https://yaladnich.github.io/meridian-prototype/ (`index.html`, збирається скриптом; підшипник у «Послугах» — SVG) |
| v2 з відео | …/video.html — той самий підшипник відео WebM, для порівняння |
| v1 | https://yaladnich.github.io/meridian-prototype/v1.html (`v1.html`, джерело для збирання) |
| старі посилання | …/v2.html і …/svg.html — перенаправляють на головну |
| Репозиторій | `yaladnich/meridian-prototype`, публічний, гілка `main`, GitHub Pages з кореня |
| Локально | `C:\Users\Yalad\meridian-prototype` |
| 3D-рендери деталей | `C:\Users\Yalad\meridian-3d` (Blender + Freestyle, свій README) |
| Референс v2 | https://heronaiapp.com/ (Webflow, агенція Bearplus) |

Доступ: push у `main` працює через Git Credential Manager (акаунт yaladnich), `gh` залогінений.
Pages перезбирається сам за ~30 с після push, стан: `gh api repos/yaladnich/meridian-prototype/pages/builds/latest --jq .status`.

Пошук за номером або назвою працює по реальному каталогу (1 237 товарів, 16 виробників). Решта кнопок і посилань —
заглушки (`href="#"`). Ціни в картках орієнтовні з інших магазинів. Фото техніки — Wikimedia Commons CC BY-SA,
автори у футері («Автори фото»).

## Файли

```
v1.html               v1, один файл ~466 КБ (правиться руками; джерело для v2)
index.html            v2 — головна (підшипник у SVG), ЗГЕНЕРОВАНИЙ — руками не правити
video.html            та сама v2, підшипник відео WebM, ЗГЕНЕРОВАНИЙ
svg.html              перенаправлення на головну
v2.html               перенаправлення на головну (старі посилання)
assets/               лого, фото брендів bp-*.webp, фото товарів p*.webp
assets/v2/            ресурси v2 (генеруються tools/v2_assets.py), ~2,3 МБ
tools/build_v2.py     v1.html → index.html (SVG) + video.html
tools/v2_assets.py    грейд і креслення фото, креслення й рендери деталей, відео з альфою
tools/shoot.py        знімки headless Chrome (див. «Перевірка»)
```

### Анатомія v1.html (до 24.09.2026 — index.html)

- Рядки 11–460 — увесь CSS. Токени на `.dw`: `--paper #F1F0EB`, `--p2 #E9E8E2`, `--ink #262826`, `--grey #66665F`,
  `--line #C9C8C1`, `--line2 #B3B2AB`, `--ai #141715`; акцент `--acc #52AE32` заданий інлайном на `<div class="dw">`.
  Шрифти Geist + Geist Mono (Google Fonts).
- Рядок 465 — уся розмітка одним рядком (знімок DOM, ~344 тис. символів). У ній ДВІ копії сторінки: `.dsk` і `.mob`
  (`.mob` видно при ширині ≤ 899 px). Будь-яка правка розмітки — в обох копіях.
- Рядки 468–649 — скрипт: `M` (16 виробників), `RAW`/`CAT` (1 237 товарів: id, назва, індекс виробника, артикул,
  інші номери), `PRICES`, `search()`, слайдер категорій, маска телефону, поява під час прокрутки, паралакс.

Порядок секцій і мітки: `hero` → `intro` (h1) → біжучий рядок `.mq` → `[01] Пошук` → `[02] Склад` (`.stmt`, слайдер) →
`[03] Типи деталей` → `[04] Виробники` → `[05] Каталог` → `[06] Умови роботи` → `[07] Під замовлення` → футер.

Словник класів, який варто знати перед правками:

- `data-rv` / `data-sec` → IntersectionObserver додає `.in`; на `.in` запускаються анімації (часто через `animation-play-state`).
- `.mk` + `.tg` — виноска: зелений квадрат і чорна плашка з лідером (`.tg.lf` — плашка ліворуч). Затримка через `--d`.
- `.rl` / `.rw` / `.ch` — «прокрутка літер» при наведенні. Розмітка згенерована (кожна літера двічі), нові написи
  робити функцією `roll()` з `build_v2.py`.
- `.hv` + `.tbg` — заливка знизу при наведенні.
- `.drw .dr` — SVG-лінії, що креслять себе (`pathLength="1"`, `stroke-dasharray:1`).
- `.ru` лінійки, `.cr` хрестики `+`, `.sl` лінія секції, `.lrow` рядок-заголовок секції.

## v2: що зроблено

Усе з референсу Heron, крім Lenis/Barba (плавна прокрутка й переходи між сторінками не брались).
v2 — основа майбутнього редизайну живого сайту (рішення 24.09.2026).

Порядок секцій v2: герой-слайдер виробників → `intro` (h1) → `[01] Каталог` (з пошуком) → рядок логотипів виробників →
`[02] Склад` → `[03] Категорії запчастин` → `[04] Послуги` → `[05] Умови роботи` → `[06] Під замовлення` → футер.
Секцію v1 «[04] Виробники» прибрано (її фото стали героєм), окремий блок «Пошук» прибрано (поле тепер у каталозі),
«Типи деталей» перейменовано на «Категорії запчастин» («Усі типи» → «Усі категорії»).

1. **Герой — слайди виробників** (`section.hero.v3h`, дані `SLIDES` у `build_v2.py`). 7 слайдів: John Deere, CLAAS,
   Bednar, Geringhoff, OLIMAC, Kuhn, AMAZONE. Кожен слайд `.hs`: чорно-біле фото `.hl` (CSS-фільтр grayscale на тому ж
   файлі) відкривається зліва направо з лінією відсотків `.bpl` і лічильником `.bpn`, потім смуга від центру (`.bnd`)
   проявляє кольорове фото в шарі `.hc`, потім виноски `.mk`/`.tg` на вузлах. Перший слайд при завантаженні: `--t0 .15s`, `--tw 2.3s` (разом із «boot»-рядками
   прелоадера), наступні: `--tw .9s`. Попередній слайд (`.pv`) лишається під новим, поки той проявляється.
   - Фото вписане в рамку 3:2 `.hb` шириною `max(100%, --hh × 1,5)`, тому координати виносок — у відсотках фото
     й не з'їжджають на різних ширинах (`--hh` = висота фото: clamp(460px, 40vw, 580px), телефон 400 px).
   - Виноски — реальні категорії з каталогу бренду (напр. Bednar: Диски · 4, Долота · 5, Кронштейни · 15;
     AMAZONE: Диски · 3, Амортизатори · 4). На телефоні по одній (`mt`). Не ставити нижче ~76 % (стрілки внизу).
   - Під фото — рядок виробників `.hstr` (7 кнопок `.hbc`: мініатюра `bp-*-320.webp`, назва, кількість товарів із `CAT`).
     Активна кнопка кольорова зі смужкою часу `.hbp`, решта ч/б; клік перемикає слайд. На телефоні рядок гортається
     вбік і сам доводить активну кнопку в поле зору.
   - Кнопки рядка виробників `.hbc`: при наведенні сірий фон, кольорова мініатюра й прокрутка літер назви (без зеленої заливки).
   - Блок із заголовком під героєм (`.intro`) на ПК — сітка 4 : 2 : 1, щоб його лінії збігалися з 7 колонками рядка виробників.
   - На фото, лівий низ — картка бренду `.hp` (уся картка — посилання: наведення анімує кнопку «Запчастини …»): «Виробник [01]», назва, кількість товарів, підпис фото, посилання
     «Запчастини …» (340 px; ≤ 1240 px — 300 px; на телефоні без шапки й підпису). «01 / 07» і стрілки `.hct` —
     правий низ (телефон — правий верх). Виноски тримати поза карткою: на 1024 вона займає x < 33 %, y > 54 % фото.
   - Прелоадер першого слайда: картка «Meridian Parts · аркуш 01 / Каталог запчастин» (`.intro-t`) і список «001 Каталог…»
     (`.boot`) оформлені як картки (темна шапка / рамка, відступи); `.intro-t` стоїть там, де потім з'являється картка
     бренду, тож одна плавно змінює іншу. На телефоні `.intro-t` унизу на всю ширину, `.boot` — праворуч угорі.
   - Автопрокрутка: проявлення ~2 с + 5 с показу; пауза під мишею; свайп по фото на телефоні. Картинки наступного й
     попереднього слайдів довантажуються з `data-src`; на далекий слайд перемикає лише після `img.decode()`.
2. **Каталог одразу під героєм, пошук — усередині каталогу** (`section.cat#search`). Зверху поле `#q` / `#mq` і
   приклади-підказки (розмітка перенесена з блоку пошуку v1), нижче кнопки виробників. Набір від 2 символів
   (затримка 150 мс, Enter — одразу) або вибір виробника перемикає каталог у режим `.fx`: статична вітрина з 16 товарів
   і пагінатор ховаються, у `.cdyn .cgr` малюються картки з усього `CAT` (оцінка збігів — як у `search()` v1: артикул,
   інші номери, назва). По 12 карток (телефон 8), «Показати ще», рядок «Показано N з M». Збіг підсвічується `<mark>`.
   Лічильники на кнопках виробників показують кількість результатів запиту; заголовок — «Знайдено N товарів · бренд».
   Картка результату — клон першої статичної картки: фото `assets/p{id}.webp`, якщо воно є (16 товарів), інакше
   3D-рендер за назвою (підшипник / шестерня / зірочка → `t-*.webp`) або «Фото на запит»; ціна з `PRICES`,
   інакше «Ціна за запитом». Порожній результат — повідомлення з телефоном і посиланням на заявку.
   Під каталогом — **рядок логотипів** усіх 16 виробників без кількостей (`.lgs`, `logo_strip()`): за кількістю товарів,
   6 клітинок у полі зору (телефон 2), стрілки, автопрокрутка 3,2 с, пауза під мишею. Логотипи одним кольором `--ink`,
   при наведенні — кольорові; висота за візуальною площею (≈3600 px², ≤ 40 px заввишки, ≤ 170 px завширшки).
   Логотипи: CLAAS, Geringhoff, Horsch, Kuhn, Kverneland, AMAZONE, Parker, Väderstad — Wikimedia Commons (суспільне
   надбання); BEDNAR (Cz-bd-1) і OLIMAC (Agromacintosh) — Commons, CC BY-SA 4.0, автори у футері («Автори фото»);
   Optibelt — сайт optibelt.com (CDN storyblok), Schumacher — groupschumacher.com, John Deere — cdn-ux.deere.com
   (з файлу взято зелену версію), Gates — gates.com (чорна версія; сайт блокує прямі запити, файл узято через браузер;
   кольорова для наведення перефарбована у фірмовий #BF2026 із сайту), A&I Products — aiproducts.com (PNG 166×65, трохи
   м'який). AGV — назвою: це власна лінія Bepco, окремого файлу логотипа немає. Вихідники — `assets/logos/src/`.
3. **«[04] Послуги»** (`section.story.sv`, дані `STEPS`): підшипник із `meridian-3d`, що грає сам, коли блок на екрані.
   - Креслення `bearing-l.webp` відкривається (0,2–1,8 с) з лінією відсотків, смуга від центру проявляє рендер
     `bearing.webp` (1,8–2,9 с), далі відео WebM VP9 з альфою (`bearing-1600.webm`, на ≤ 899 px — `bearing-960.webm`,
     вантажиться, коли блок за 300 px від екрана). Класи `.v2h.vid` (відео грає, креслення — «привид» 35 %),
     `.v2h.ex` (t 1,85–7,15 с — підшипник розібраний, виноски ховаються). Поза екраном відео на паузі.
   - Safari (UA без Chrome/Firefox) відео не отримує — лишається статичний рендер.
   - Виноски (у % кадру 16:10): «Підшипники · 85» (25, 66, ліворуч), «Ролики циліндричні × 16» (29,5, 42),
     «Кільце зовнішнє» (64, 24,5), «Кільце внутрішнє» (66,5, 44). На телефоні одна: «Підшипники · 85» (60, 24).
   - Три кроки (Постачання, Консультації, Установка) змінюються самі кожні 5 с, поки блок на екрані: великий заголовок
     по центру, картка ліворуч унизу зі смужкою часу, прогрес праворуч, лічильник «01 / 03». Тексти — з поточного сайту
     й блоку «Умови роботи».
4. **«Умови роботи»:** над колонками ізометричні іконки (кільце, коробка, аркуші, розвідний ключ) — SVG-лінії, генеруються
   в `build_v2.py` (не Blender), креслять себе на `.facts.in`, приховані ребра пунктиром, зелений квадрат. Кожна колонка —
   картка: при наведенні фон, іконка піднімається, пунктир «біжить», квадрат збільшується, посилання внизу отримує
   фірмовий ховер; клік по картці = клік по її посиланню.
5. **Блок замовлення:** координати кутів `[0,0]`, `[1416,414]`… (на телефоні сховані), приціл і `[x,y]` за курсором.
6. **Зерно паперу** — SVG feTurbulence на фіксованому шарі `.dw::after`, opacity .38.
7. **Шапка ≤ 1400 px** компактна (лого 220 px, пункти меню 112 px, вужчі відступи телефону, месенджерів, кошика) —
   інакше на 1241–1400 і на 1024 лічильник кошика обрізається (у v1 це лишилось).
8. **Ховери:** «Замовити» в картках товарів і «Показати ще» — з прокруткою літер; іконки месенджерів у футері
   анімуються плавно, як у шапці (правило `.ft4 a` перебивало їм transition).
9. Мітки секцій перенумеровано. Посилань між версіями у футері немає — версії відкриваються за адресами.

### Підшипник у SVG (головна) і відео (`video.html`)

`build_v2.py` збирає дві сторінки: `index.html` — у «Послугах» SVG (`bearing_svg()` + скрипт `SVG_JS`), `video.html` — відео WebM
(`bearing()`). Різниця лише в цьому блоці.
- Геометрія — з моделі Blender (`meridian-3d/scripts/meridian.py → build_bearing`): **конічний** роликопідшипник 35×72×18,25,
  зовнішнє кільце (z 3,25–18,25, r 36 / отвір 29,2, доріжка до 32,4 унизу), внутрішнє кільце (отвір 17,5, торець 21,95,
  бурт 26), 16 конічних роликів (вісь нахилена на 14°, торці r 4,0 / 4,6). Кольори й освітлення як у рендері: три тони
  #ECEBE6 / #CFCEC8 / #9C9B96, контур #262826, світло з азимуту 195°; орто-проєкція під 30°, 1 мм = 10 одиниць viewBox.
- Освітлення як у Blender: дифузне світло з того самого напрямку (у системі камери (−0,369; −0,240; 0,898)) і та сама
  toon-шкала з м'якими переходами: темний до 0,10, перехід до 0,15, середній до 0,55, перехід до 0,60, світлий. Кожна
  поверхня тіла обертання — смуга з лінійним градієнтом тону за кутом (звідси напівтони), торці — світлі.
- Скрипт будує кільця один раз (фіксовані шляхи, рухаються через `transform`), ролики (16 граней з тоном за нормаллю,
  торець з фаскою) і перемички сепаратора перераховує щокадру. Сепаратор — 16 секторів ободів, фланець і 16 перемичок,
  кожен окремим шаром; порядок малювання — сортування за глибиною щокадру. Внутрішнє кільце має «тіло» (опукла
  оболонка профілю) з наскрізним отвором. `box.brgPose(t)` зупиняє анімацію на моменті t (для порівняння з кадрами).
- Перекриття зовнішнім кільцем — маски `clipPath` (`brcin-*`, `brclo-*`), що рахуються щокадру як у рендері: деталь
  усередині кільця видно лише крізь верхній отвір (перетин еліпсів r 28,4 на z 17,35 і r 29,2 на z 18,25); деталь нижче
  кільця — крізь обидва отвори (ще й нижній r 33,1) або повз корпус (поза опуклою оболонкою циліндра r 36). Маска
  призначається роликам, сепаратору й внутрішньому кільцю за їхньою верхньою точкою відносно низу кільця. Без цього
  ролики з боків лягали поверх краю кільця.
- Ролики торкаються доріжок обох кілець точно по дотичній (перевірено розрахунком за профілями з моделі), нічого не врізається.
  Сепаратор: у кожного з 16 секторів верхній обід із фланцем і нижній обід — окремі шари; верхній обід із фланцем лежить
  над внутрішнім кільцем і торцями роликів (ролики заходять під нього у вікна) — завжди поверх них і під зовнішнім кільцем;
  тон сегментів плавно змінюється (градієнт), без сходинок. Сепаратор має власну, темнішу палітру (штампована сталь,
  #D6D5CF / #B6B5AF / #88877F), інакше зливається з внутрішнім кільцем і здається, що ролики входять у кільце
  (так виглядає відео з Blender, де сепаратор того самого матеріалу).
- Ролики: бічна поверхня — лінійний градієнт тону поперек ролика (14 зупинок), контури по 36 точках; лінії 1,3 / 0,8
  (на телефоні 2 / 1,2) — як у рендері відносно розміру деталі.
- Сценарій як у рендері, цикл 8 с: обертання, кільце +20 мм, внутрішнє −20 мм, ролики +2,5 мм назовні, камера ×1,35.
  Спершу лінії «креслять себе» (рентгеном, без заливок), потім смуга від центру проявляє заливки.
- Виноски `.ca[data-a]` скрипт щокадру ставить у точки деталей — під час розбору їх не треба ховати.
- Вага: скрипт ~13 КБ проти відео 573 КБ + два WebP (~90 КБ); працює і в Safari, чіткий на будь-якому екрані.
- Підпис роликів виправлено в обох версіях: «Ролики конічні × 16» (було «циліндричні» — модель конічна).

## Перенесення на живий сайт: дані лише з бази

Прототип бере дані зі знімка каталогу у `v1.html` (`M`, `RAW`/`CAT`, `PRICES`) і з чисел, вписаних у `build_v2.py`.
На сайті (WordPress + WooCommerce) усе, що показують картки й лічильники, має братися з бази товарів і оновлюватися
саме, без ручних правок:

- **Картки товарів** (каталог, результати пошуку): назва, артикул (SKU), інші номери / аналоги, виробник, ціна,
  наявність (stock status), фото з галереї товару, посилання на товар і кнопки «Замовити» / «В кошик».
- **Кількості**: «1 237 товарів», «16 виробників», лічильники на кнопках виробників, кількість у рядку виробників
  героя й у картці бренду, у «Категоріях запчастин» — рахуються запитом до бази
  (виробник — таксономія плагіна woo-ik-manufacturer, категорії — product_cat).
- **Виноски на фото героя** («Долота · 5», «Підшипники · 10»): позиція на фото задається вручну, а напис і число —
  категорія + виробник з бази; якщо в бренду 0 товарів у категорії, виноска не показується.
- **Пошук** — по базі (WooCommerce REST / AJAX-ендпоінт), ті самі правила: точний артикул, початок артикула,
  інші номери, частина назви; результати сторінками.
- **Ціни**: у прототипі 16 орієнтовних цін з інших магазинів — на сайті лише реальні ціни WooCommerce.

### Як збирається v2

```
python tools/v2_assets.py          # усе; або brands | parts | video
python tools/build_v2.py           # v1.html → index.html
```

- `v2_assets.py logos` робить з `assets/logos/src/*` одноколірні `logo-*.webp` (білий фон вибито; для Väderstad, BEDNAR,
  John Deere й A&I — за яскравістю (поріг 0,62): жовте й світло-зелене вибивається, темне лишається) і кольорові `logo-*-c.webp`, розміри — `logos.json`.
- `v2_assets.py` бере `assets/bp-*.webp` (фото героя: `bp-*.webp` до 1600 px, `bp-*-800.webp` для телефона,
  `bp-*-320.webp` — мініатюри рядка) і з `C:\Users\Yalad\meridian-3d` — `out/*-poster.png` та кадри
  `frames/hero-bearing/0000–0191.png`. Грейд «чистий, соковитий» (варіант B, обраний 24.09.2026 з чотирьох):
  насиченість ×1,12, м'яка S-крива 25 %, трохи холодніші тіні. Ч/б стан — лише CSS-фільтр, окремих файлів немає. Контури рендерів — поріг яскравості (лінії Freestyle #262826).
  Відео: ffmpeg libvpx-vp9 yuva420p, crf 36.
- `build_v2.py` робить заміни в `.dsk` і `.mob` окремо через `sub1()`, яка перевіряє кількість збігів. Якщо v1 змінили
  й якір зник — скрипт падає з назвою шаблону, тоді оновити шаблон. Нові CSS і JS дописуються в кінець `<style>` і
  окремим `<script>` після основного.
- Правка `v1.html` → завжди перезібрати `index.html` і перевірити обидві.

## Перевірка

- Локальний сервер: конфігурація `meridian` у `C:\Users\Yalad\.claude\launch.json` (порт 8765), або
  `python -m http.server 8765` у корені репозиторію.
- Ширини: 1440×900, 1024×768, 390×844. Горизонтальної прокрутки немає: `document.documentElement.scrollWidth === innerWidth`.
- Панель браузера застосунку часто не малює після прокрутки й на відео — знімати `tools/shoot.py` (знімки в `shots/`, у git не йдуть):
  ```
  python tools/shoot.py http://localhost:8765/ 1440 900 plan.json
  ```
  Крок `["HOVER:.dsk .hp.on", 0.8, "name"]` наводить мишку на елемент (для перевірки ховерів); `SHOOT_DPR=2` — знімок удвічі щільніший.
- Смоук-тест: пошук `RE154` → «Знайдено 1 товар»; немає відповідей 4xx у `performance.getEntriesByType('resource')`;
  у героях 7 слайдів перемикаються стрілками (`.dsk [data-hs="1"]`); у «Послугах» кроки міняються за 5 с.

## Відомі обмеження й відкрите

- Safari: без відео в героях v2 (статичний рендер). Для повного паритету потрібен HEVC з альфою (кодується лише на macOS).
- Фото CLAAS лише 864 px завширшки — у героях на ПК трохи м'якше за інші. Оригінал на Wikimedia більший (завантажити за згоди).
- v1 (не виправлено): лічильник кошика обрізаний на 1024 і 1241–1400 px, «А–А» налазить на рядок брендів у героях;
  на 390 px плашка CLAAS «Підшипники · 10» виходить за край.
- На 1024 обрізається підказка в полі пошуку (обидві версії).
- У стрічці є виробники з 2–3 товарами (A&I, AGV, Kverneland) — можливо, варто показувати лише від N товарів.
- Нові тексти не вигадувати без погодження: спершу 2–3 варіанти на вибір.

## Журнал версій

| Дата | Що | Коміт |
|---|---|---|
| до 23.09.2026 | v1: прототип каталогу, пошук, слайдер категорій, умови роботи | `6fb0251`…`5fa5639` |
| 23.09.2026 | Клон у `C:\Users\Yalad\meridian-prototype`; v2 за референсом Heron (герой, бренди, «Послуги», іконки, стрічка, координати, зерно) | `66442bb` |
| 24.09.2026 | `BRIEF.md`, `CLAUDE.md`, `tools/shoot.py`, `.gitignore` | `68ea15d` |
| 24.09.2026 | v2: герой — слайди виробників (креслення → фото, виноски з категорій каталогу); каталог під героєм; «Послуги» — підшипник грає сам, кроки за таймером; секцію «Виробники» прибрано; компактна шапка ≤ 1400 px | `d6f438c` |
| 24.09.2026 | Герой: рядок виробників під фото замість плашки бренду; початковий стан ч/б замість креслення; грейд B «чистий, соковитий» | `2f45d1d` |
| 24.09.2026 | Картку бренду на фото героя повернуто (разом із рядком виробників) | `cb728b3` |
| 24.09.2026 | Пошук перенесено в каталог (фільтрує сітку по всьому каталогу разом із виробником); «Категорії запчастин»; картки прелоадера з відступами; розділ «Перенесення на живий сайт: дані лише з бази» | `1f700c8` |
| 24.09.2026 | Стрічку виробників з назвами й кількостями замінено рядком логотипів 16 виробників (одним кольором, кольорові при наведенні) | `bd61f40` |
| 24.09.2026 | v2 стала головною (`index.html`), v1 → `v1.html`, `v2.html` перенаправляє на головну | `ec0bf86` |
| 24.09.2026 | Фірмовий ховер: бренди в героях, картка бренду-посилання, «Замовити», футер; «Умови роботи» як картки; іконка «Сервіс» — розвідний ключ; `shoot.py` уміє `HOVER:` | `7e738e5` |
| 24.09.2026 | Лінії блоку із заголовком вирівняно з рядком виробників; у кнопок виробників без зеленої заливки | `05a5d17` |
| 24.09.2026 | «[02] Склад»: кнопка «Замовити запчастини» 40 px і на одній лінії з навігацією слайдера (відступ знизу 14 px) | `24e127a` |
| 24.09.2026 | Логотипи John Deere, Gates, A&I з офіційних сайтів; назвою лишився тільки AGV | `50ac71c` |
| 24.09.2026 | Окрема сторінка `svg.html`: підшипник у «Послугах» — SVG-рендерер замість відео; «Ролики конічні» | `7429e9a` |
| 24.09.2026 | SVG-підшипник доведено до рендера: освітлення й toon-шкала Blender з градієнтами, фаски, сепаратор, грані роликів; SVG — на головній, відео → `video.html` | `ec0aea1` |
| 24.09.2026 | SVG-підшипник: маски перекриття зовнішнім кільцем — ролики з боків ховаються під край, як у рендері; `shoot.py` — `SHOOT_DPR` | `96662cb` |
| 24.09.2026 | SVG-підшипник: сепаратор окремими шарами й власним тоном, плавне освітлення роликів, тонші лінії | `900183f` |
| 24.09.2026 | Прибрано посилання на інші версії з футера | див. git log |
