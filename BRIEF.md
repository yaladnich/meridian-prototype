# Meridian Parts — прототип редизайну каталогу

Бриф для нової сесії: що це, як влаштовано, що зроблено, як перевіряти. Після кожної правки оновлювати
відповідний розділ і рядок у «Журналі версій» унизу.

## Що це

Прототип дизайну каталогу запчастин для сільгосптехніки [meridianparts.com.ua](https://www.meridianparts.com.ua/)
(склад-магазин у Києві). На головній поточного сайту частина блоків — тексти-заглушки шаблону, тому тексти
прототипу беруться з реальних даних магазину (каталог, умови доставки й оплати, послуги).

| | |
|---|---|
| v1 | https://yaladnich.github.io/meridian-prototype/ (`index.html`) |
| v2 | https://yaladnich.github.io/meridian-prototype/v2.html (`v2.html`, збирається скриптом) |
| Репозиторій | `yaladnich/meridian-prototype`, публічний, гілка `main`, GitHub Pages з кореня |
| Локально | `C:\Users\Yalad\meridian-prototype` |
| 3D-рендери деталей | `C:\Users\Yalad\meridian-3d` (Blender + Freestyle, свій README) |
| Референс v2 | https://heronaiapp.com/ (Webflow, агенція Bearplus) |

Доступ: push у `main` працює через Git Credential Manager (акаунт yaladnich), `gh` залогінений.
Pages перезбирається сам за ~30 с після push, стан: `gh api repos/yaladnich/meridian-prototype/pages/builds/latest --jq .status`.

Пошук за номером або назвою працює по реальному каталогу (1 237 товарів, 16 виробників). Решта кнопок і посилань —
заглушки (`href="#"`). Ціни в картках орієнтовні з інших магазинів. Фото техніки — Wikimedia Commons CC BY-SA,
автори у футері («Автори фото»).

## Файли

```
index.html            v1, один файл ~466 КБ (правиться руками)
v2.html               v2, ЗГЕНЕРОВАНИЙ — руками не правити
assets/               лого, фото брендів bp-*.webp, фото товарів p*.webp
assets/v2/            ресурси v2 (генеруються tools/v2_assets.py), ~2,3 МБ
tools/build_v2.py     index.html → v2.html
tools/v2_assets.py    грейд і креслення фото, креслення й рендери деталей, відео з альфою
tools/shoot.py        знімки headless Chrome (див. «Перевірка»)
```

### Анатомія index.html

- Рядки 11–460 — увесь CSS. Токени на `.dw`: `--paper #F1F0EB`, `--p2 #E9E8E2`, `--ink #262826`, `--grey #66665F`,
  `--line #C9C8C1`, `--line2 #B3B2AB`, `--ai #141715`; акцент `--acc #52AE32` заданий інлайном на `<div class="dw">`.
  Шрифти Geist + Geist Mono (Google Fonts).
- Рядок 465 — уся розмітка одним рядком (знімок DOM, ~344 тис. символів). У ній ДВІ копії сторінки: `.dsk` і `.mob`
  (`.mob` видно при ширині ≤ 899 px). Будь-яка правка розмітки — в обох копіях.
- Рядки 468–649 — скрипт: `M` (16 виробників), `RAW`/`CAT` (1 237 товарів: id, назва, індекс виробника, артикул,
  інші номери), `PRICES`, `search()`, слайдер категорій, маска телефону, поява під час прокрутки, паралакс.

Порядок секцій і мітки: `hero` → `intro` (h1) → біжучий рядок `.mq` → `[01] Пошук` → `[02] Склад` (`.stmt`, слайдер) →
`[03] Типи деталей` → `[04] Виробники` → `[05] Каталог` → `[06] Умови роботи` → `[07] Під замовлення` → футер.

Словник класів, який варто знати перед правками:

- `data-rv` / `data-sec` → IntersectionObserver додає `.in`; на `.in` запускаються анімації (часто через `animation-play-state`).
- `.mk` + `.tg` — виноска: зелений квадрат і чорна плашка з лідером (`.tg.lf` — плашка ліворуч). Затримка через `--d`.
- `.rl` / `.rw` / `.ch` — «прокрутка літер» при наведенні. Розмітка згенерована (кожна літера двічі), нові написи
  робити функцією `roll()` з `build_v2.py`.
- `.hv` + `.tbg` — заливка знизу при наведенні.
- `.drw .dr` — SVG-лінії, що креслять себе (`pathLength="1"`, `stroke-dasharray:1`).
- `.ru` лінійки, `.cr` хрестики `+`, `.sl` лінія секції, `.lrow` рядок-заголовок секції.

## v2: що зроблено

Усе з референсу Heron, крім Lenis/Barba (плавна прокрутка й переходи між сторінками не брались).
v2 — основа майбутнього редизайну живого сайту (рішення 24.09.2026).

Порядок секцій v2: герой-слайдер виробників → `intro` (h1) → `[01] Каталог` → стрічка виробників → `[02] Пошук` →
`[03] Склад` → `[04] Типи деталей` → `[05] Послуги` → `[06] Умови роботи` → `[07] Під замовлення` → футер.
Секцію v1 «[04] Виробники» прибрано: її фото стали героєм.

1. **Герой — слайди виробників** (`section.hero.v3h`, дані `SLIDES` у `build_v2.py`). 7 слайдів: John Deere, CLAAS,
   Bednar, Geringhoff, OLIMAC, Kuhn, AMAZONE. Кожен слайд `.hs`: чорно-біле фото `.hl` (CSS-фільтр grayscale на тому ж
   файлі) відкривається зліва направо з лінією відсотків `.bpl` і лічильником `.bpn`, потім смуга від центру (`.bnd`)
   проявляє кольорове фото в шарі `.hc`, потім виноски `.mk`/`.tg` на вузлах. Перший слайд при завантаженні: `--t0 .15s`, `--tw 2.3s` (разом із «boot»-рядками
   прелоадера), наступні: `--tw .9s`. Попередній слайд (`.pv`) лишається під новим, поки той проявляється.
   - Фото вписане в рамку 3:2 `.hb` шириною `max(100%, --hh × 1,5)`, тому координати виносок — у відсотках фото
     й не з'їжджають на різних ширинах (`--hh` = висота фото: clamp(460px, 40vw, 580px), телефон 400 px).
   - Виноски — реальні категорії з каталогу бренду (напр. Bednar: Диски · 4, Долота · 5, Кронштейни · 15;
     AMAZONE: Диски · 3, Амортизатори · 4). На телефоні по одній (`mt`). Не ставити нижче ~76 % (стрілки внизу).
   - Під фото — рядок виробників `.hstr` (7 кнопок `.hbc`: мініатюра `bp-*-320.webp`, назва, кількість товарів із `CAT`).
     Активна кнопка кольорова зі смужкою часу `.hbp`, решта ч/б; клік перемикає слайд. На телефоні рядок гортається
     вбік і сам доводить активну кнопку в поле зору.
   - На фото, лівий низ — картка бренду `.hp`: «Виробник [01]», назва, кількість товарів, підпис фото, посилання
     «Запчастини …» (340 px; ≤ 1240 px — 300 px; на телефоні без шапки й підпису). «01 / 07» і стрілки `.hct` —
     правий низ (телефон — правий верх). Виноски тримати поза карткою: на 1024 вона займає x < 33 %, y > 54 % фото.
   - Автопрокрутка: проявлення ~2 с + 5 с показу; пауза під мишею; свайп по фото на телефоні. Картинки наступного й
     попереднього слайдів довантажуються з `data-src`; на далекий слайд перемикає лише після `img.decode()`.
2. **Каталог одразу під героєм**, далі стрічка виробників (5 клітинок, телефон 2, стрілки, автопрокрутка 3,2 с;
   кількість товарів — з `CAT`).
3. **«[05] Послуги»** (`section.story.sv`, дані `STEPS`): підшипник із `meridian-3d`, що грає сам, коли блок на екрані.
   - Креслення `bearing-l.webp` відкривається (0,2–1,8 с) з лінією відсотків, смуга від центру проявляє рендер
     `bearing.webp` (1,8–2,9 с), далі відео WebM VP9 з альфою (`bearing-1600.webm`, на ≤ 899 px — `bearing-960.webm`,
     вантажиться, коли блок за 300 px від екрана). Класи `.v2h.vid` (відео грає, креслення — «привид» 35 %),
     `.v2h.ex` (t 1,85–7,15 с — підшипник розібраний, виноски ховаються). Поза екраном відео на паузі.
   - Safari (UA без Chrome/Firefox) відео не отримує — лишається статичний рендер.
   - Виноски (у % кадру 16:10): «Підшипники · 85» (25, 66, ліворуч), «Ролики циліндричні × 16» (29,5, 42),
     «Кільце зовнішнє» (64, 24,5), «Кільце внутрішнє» (66,5, 44). На телефоні одна: «Підшипники · 85» (60, 24).
   - Три кроки (Постачання, Консультації, Установка) змінюються самі кожні 5 с, поки блок на екрані: великий заголовок
     по центру, картка ліворуч унизу зі смужкою часу, прогрес праворуч, лічильник «01 / 03». Тексти — з поточного сайту
     й блоку «Умови роботи».
4. **«Умови роботи»:** над колонками ізометричні іконки (кільце, коробка, аркуші, гайка) — генеруються в `build_v2.py`,
   креслять себе на `.facts.in`, приховані ребра пунктиром, зелений квадрат.
5. **Блок замовлення:** координати кутів `[0,0]`, `[1416,414]`… (на телефоні сховані), приціл і `[x,y]` за курсором.
6. **Зерно паперу** — SVG feTurbulence на фіксованому шарі `.dw::after`, opacity .38.
7. **Шапка ≤ 1400 px** компактна (лого 220 px, пункти меню 112 px, вужчі відступи телефону, месенджерів, кошика) —
   інакше на 1241–1400 і на 1024 лічильник кошика обрізається (у v1 це лишилось).
8. Мітки секцій перенумеровано; посилання між версіями — у футері «прототип v2, перша версія →».

### Як збирається v2

```
python tools/v2_assets.py          # усе; або brands | parts | video
python tools/build_v2.py           # index.html → v2.html
```

- `v2_assets.py` бере `assets/bp-*.webp` (фото героя: `bp-*.webp` до 1600 px, `bp-*-800.webp` для телефона,
  `bp-*-320.webp` — мініатюри рядка) і з `C:\Users\Yalad\meridian-3d` — `out/*-poster.png` та кадри
  `frames/hero-bearing/0000–0191.png`. Грейд «чистий, соковитий» (варіант B, обраний 24.09.2026 з чотирьох):
  насиченість ×1,12, м'яка S-крива 25 %, трохи холодніші тіні. Ч/б стан — лише CSS-фільтр, окремих файлів немає. Контури рендерів — поріг яскравості (лінії Freestyle #262826).
  Відео: ffmpeg libvpx-vp9 yuva420p, crf 36.
- `build_v2.py` робить заміни в `.dsk` і `.mob` окремо через `sub1()`, яка перевіряє кількість збігів. Якщо v1 змінили
  й якір зник — скрипт падає з назвою шаблону, тоді оновити шаблон. Нові CSS і JS дописуються в кінець `<style>` і
  окремим `<script>` після основного.
- Правка v1 → завжди перезібрати v2 і перевірити обидві.

## Перевірка

- Локальний сервер: конфігурація `meridian` у `C:\Users\Yalad\.claude\launch.json` (порт 8765), або
  `python -m http.server 8765` у корені репозиторію.
- Ширини: 1440×900, 1024×768, 390×844. Горизонтальної прокрутки немає: `document.documentElement.scrollWidth === innerWidth`.
- Панель браузера застосунку часто не малює після прокрутки й на відео — знімати `tools/shoot.py` (знімки в `shots/`, у git не йдуть):
  ```
  python tools/shoot.py http://localhost:8765/v2.html 1440 900 plan.json
  ```
- Смоук-тест: пошук `RE154` → «Знайдено 1 товар»; у стрічці 16 виробників; немає відповідей 4xx у `performance.getEntriesByType('resource')`;
  у героях 7 слайдів перемикаються стрілками (`.dsk [data-hs="1"]`); у «Послугах» кроки міняються за 5 с.

## Відомі обмеження й відкрите

- Safari: без відео в героях v2 (статичний рендер). Для повного паритету потрібен HEVC з альфою (кодується лише на macOS).
- Фото CLAAS лише 864 px завширшки — у героях на ПК трохи м'якше за інші. Оригінал на Wikimedia більший (завантажити за згоди).
- v1 (не виправлено): лічильник кошика обрізаний на 1024 і 1241–1400 px, «А–А» налазить на рядок брендів у героях;
  на 390 px плашка CLAAS «Підшипники · 10» виходить за край.
- На 1024 обрізається підказка в полі пошуку (обидві версії).
- У стрічці є виробники з 2–3 товарами (A&I, AGV, Kverneland) — можливо, варто показувати лише від N товарів.
- Нові тексти не вигадувати без погодження: спершу 2–3 варіанти на вибір.

## Журнал версій

| Дата | Що | Коміт |
|---|---|---|
| до 23.09.2026 | v1: прототип каталогу, пошук, слайдер категорій, умови роботи | `6fb0251`…`5fa5639` |
| 23.09.2026 | Клон у `C:\Users\Yalad\meridian-prototype`; v2 за референсом Heron (герой, бренди, «Послуги», іконки, стрічка, координати, зерно) | `66442bb` |
| 24.09.2026 | `BRIEF.md`, `CLAUDE.md`, `tools/shoot.py`, `.gitignore` | `68ea15d` |
| 24.09.2026 | v2: герой — слайди виробників (креслення → фото, виноски з категорій каталогу); каталог під героєм; «Послуги» — підшипник грає сам, кроки за таймером; секцію «Виробники» прибрано; компактна шапка ≤ 1400 px | `d6f438c` |
| 24.09.2026 | Герой: рядок виробників під фото замість плашки бренду; початковий стан ч/б замість креслення; грейд B «чистий, соковитий» | `2f45d1d` |
| 24.09.2026 | Картку бренду на фото героя повернуто (разом із рядком виробників) | див. git log |
